#use html5_elements.json
#html5_elements.txt
#17:42 13/12/2013
#HTML5 Elements:
# structure:
#0fucntion -which function does the element belong to; use ---Function group name before the element
#1element -the name of the element in lowercase;
#2html version -since when it this element there ? e>2 or e>4? [++ is it deprecated ? 5>e>2 (since 2 until 5)]
#3void element or not void- e.g. (img,source and meta are void elements) write nv for not void and v for void
#4must attributes (e.g. src= and alt= for img element)
#5specific attributes and not must
#6standard parent (head body ...)?
#7alternatives